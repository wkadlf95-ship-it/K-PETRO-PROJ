# 대국민포털 실행 방법

## 가장 쉬운 실행 방법

프로젝트 폴더에서 `start-dev.cmd`를 더블클릭하거나 VS Code 터미널에서 아래 명령을 실행합니다.

```powershell
.\start-dev.cmd
```

패키지가 없으면 자동으로 설치한 뒤 개발 서버가 실행됩니다. 터미널에 표시되는 주소(보통 `http://localhost:5173/`)를 브라우저에서 열면 됩니다.

## VS Code 메뉴로 실행

1. `Ctrl+Shift+P`
2. `Tasks: Run Task` 선택
3. `대국민포털: 개발 서버 실행` 선택

## 종료

개발 서버가 실행 중인 터미널에서 `Ctrl+C`를 누릅니다.

## 완성본 빌드

`build-prod.cmd`를 더블클릭하거나 VS Code 작업에서 `대국민포털: 프로덕션 빌드`를 선택합니다.

## 메뉴를 하나씩 숨기는 방법

`app/config/navigation.ts`에서 숨길 메뉴의 `enabled: true`를 `enabled: false`로 바꾸면 됩니다.
