# 공공기관 사업 — 웹 취약점 · 접근성 검사 지침

## 쓰는 법

**방법 A — Claude 에게 시키기**

이 파일을 검사할 프로젝트에 두고:

```
공공-웹취약점-접근성-검사지침.md 대로 이 프로젝트를 검사하고 6장 형식으로 보고해줘
```

**방법 B — 스크립트 직접 돌리기**

5장의 스크립트를 `a11y-scan.js` 로 저장한 뒤:

```bash
node a11y-scan.js            # 현재 디렉터리
node a11y-scan.js ./src      # 특정 경로
```

의존성 없습니다. 즉시 수정(deny)이 하나라도 있으면 종료 코드 1 이라 CI 에 바로 붙습니다.

## 판정 두 가지

| 판정 | 뜻 | 처리 |
|---|---|---|
| **즉시 수정** | 정당한 경우가 사실상 없음 | 고치고 넘어간다 |
| **확인 필요** | 정당한 경우가 있음 | 한 건씩 보고 판단한다. 정당하면 사유를 남긴다 |

`확인 필요` 를 전부 고치려 들지 마세요. 장식용 이미지의 빈 `alt`, 대체 초점 스타일을
갖춘 `outline: none` 은 정당합니다. **판단을 기록으로 남기는 것**이 목적입니다.

---

## 1. 검사 대상

| 확장자 | 보는 것 |
|---|---|
| `.html` `.jsp` `.jspf` | 마크업 전반 |
| `.tsx` `.jsx` `.vue` | 컴포넌트 마크업 · 스크립트 |
| `.ts` `.js` | 스크립트 |
| `.css` `.scss` | 스타일 |

**제외**: `node_modules` `.git` `dist` `build` `out` `coverage` `vendor` `.next` `.nuxt`
`target` `.venv` `__pycache__`

---

## 2. 웹 취약점 4개

### 2.1 마크업을 문자열로 주입 — 확인 필요

XSS 가 가장 많이 나는 자리입니다.

```
정규식: dangerouslySetInnerHTML|\bv-html\b|\.innerHTML\s*=|insertAdjacentHTML\s*\(
대상  : .tsx .jsx .ts .js .vue
```

```jsx
box.innerHTML = keyword;                                  // 걸림
<div dangerouslySetInnerHTML={{__html: userInput}} />     // 걸림
<div v-html="userInput"></div>                            // 걸림
```

**조치** — 값이 외부 입력이면 `textContent` 나 JSX 보간으로 넣거나 이스케이프합니다.
고정 문자열이면 그대로 두어도 됩니다. **어느 쪽인지 판단해 기록하세요.**

### 2.2 `javascript:` URL — 즉시 수정

```
정규식: (?:href|src|action)\s*=\s*["']?\s*javascript:
대상  : .html .jsp .jspf .tsx .jsx .vue
```

```html
<a href="javascript:go()">클릭</a>          <!-- 걸림 -->
<a href="/board?tab=javascript">게시판</a>   <!-- 통과 (경로에 들어간 단어) -->
```

**조치** — 이벤트 핸들러를 붙입니다.

### 2.3 문자열을 코드로 실행 — 즉시 수정

```
정규식: \beval\s*\(|\bnew\s+Function\s*\(|document\.write\s*\(
대상  : .ts .tsx .js .jsx .vue
```

**조치** — 소프트웨어 개발보안 점검에서 지적되는 항목입니다. 다른 방식으로 바꿉니다.

### 2.4 새 창 링크에 `noopener` 없음 (탭내빙) — 확인 필요

연 쪽 창이 `window.opener` 로 원래 페이지 주소를 바꿔치기할 수 있습니다.

```
정규식: <a\b(?=[^>]*target\s*=\s*["']?_blank)(?![^>]*\brel\s*=\s*["'][^"']*noopener)[^>]*>
대상  : .html .jsp .jspf .tsx .jsx .vue
```

```html
<a href="/x" target="_blank">외부</a>                              <!-- 걸림 -->
<a href="/x" target="_blank" rel="noopener noreferrer">외부</a>     <!-- 통과 -->
```

---

## 3. 웹 접근성 7개

### 3.1 `img` 대체 텍스트 누락 — 확인 필요

접근성 심사의 첫 항목입니다.

```
정규식: <img\b(?![^>]*\balt\s*=)[^>]*>
대상  : .html .jsp .jspf .tsx .jsx .vue
```

```html
<img src="/logo.png">                    <!-- 걸림 -->
<img src="/logo.png" alt="회사 로고">     <!-- 통과 -->
<img src="/spacer.gif" alt="">           <!-- 통과 — 장식용은 빈 alt 로 명시 -->
```

**조치** — 의미가 있으면 내용을 적고, 장식용이면 `alt=""` 로 **비워서 명시**합니다.
속성을 아예 빼는 것과 빈 값을 넣는 것은 다릅니다.

### 3.2 확대 잠금 — 즉시 수정

저시력 사용자가 내용을 볼 수 없게 됩니다.

```
정규식: <meta\b[^>]*name\s*=\s*["']viewport["'][^>]*content\s*=\s*["'][^"']*(?:user-scalable\s*=\s*(?:no|0)|maximum-scale\s*=\s*1)
대상  : .html .jsp .jspf
```

```html
<meta name="viewport" content="width=device-width, user-scalable=no">    <!-- 걸림 -->
<meta name="viewport" content="width=device-width, initial-scale=1">     <!-- 통과 -->
```

### 3.3 초점 표시 제거 — 확인 필요

키보드 사용자가 지금 어디에 있는지 알 수 없게 됩니다.

```
정규식: outline\s*:\s*(?:none|0)\b
대상  : .css .scss .vue .tsx .jsx
```

```css
button:focus { outline: none; }                      /* 걸림 */
button:focus-visible { outline: 2px solid #06c; }    /* 대체 스타일이 있으면 정당 */
```

**조치** — 대체 초점 스타일이 함께 있는지 확인합니다. 없으면 넣습니다.

### 3.4 양수 `tabindex` — 확인 필요

초점 순서가 화면에 보이는 순서와 어긋납니다.

```
정규식: tab[Ii]ndex\s*=\s*["']?\{?\s*[1-9]
대상  : .html .jsp .jspf .tsx .jsx .vue
```

`0` 과 `-1` 만 씁니다. 순서가 필요하면 **마크업 순서를 바꿉니다.**

### 3.5 미디어 자동 재생 — 확인 필요

소리가 자동 재생되면 스크린리더 낭독을 덮습니다.

```
정규식: <(?:video|audio)\b[^>]*\bautoplay\b
대상  : .html .jsp .jspf .tsx .jsx .vue
```

### 3.6 깜빡이는 콘텐츠 — 즉시 수정

광과민성 발작을 유발할 수 있습니다.

```
정규식: <(?:blink|marquee)\b|text-decoration\s*:\s*blink
대상  : .html .jsp .jspf .css .scss .tsx .jsx .vue
```

### 3.7 `html` 에 `lang` 없음 — 확인 필요

스크린리더가 어느 언어로 읽을지 몰라 발음이 무너집니다.

```
정규식: <html\b(?![^>]*\blang\s*=)[^>]*>
대상  : .html .jsp
```

```html
<html>              <!-- 걸림 -->
<html lang="ko">    <!-- 통과 -->
```

---

## 4. ⚠ 기계로 잡을 수 없는 것 — 사람이 봐야 합니다

**위 11개를 통과해도 접근성을 만족한 것이 아닙니다.** 아래는 정규식으로 판정이
불가능하므로 별도로 점검하세요. **이 문서만으로 감리에 대응할 수 없습니다.**

| 점검 항목 | 보는 법 |
|---|---|
| 명도 대비 4.5:1 이상 | 브라우저 개발자도구 또는 대비 검사 도구 |
| 키보드만으로 전 기능 사용 | Tab 만으로 처음부터 끝까지 조작해 본다 |
| 초점 이동 순서가 화면 순서와 같은가 | Tab 을 눌러 가며 초점 위치를 눈으로 따라간다 |
| 스크린리더 실제 낭독 | NVDA · 센스리더 등으로 직접 들어 본다 |
| 색상만으로 정보를 전달하지 않는가 | 흑백으로 보고도 구분되는지 |
| 표의 제목 셀과 데이터 셀 연결 | `th` `scope` `headers` 구조 확인 |
| 오류 메시지가 입력란과 연결되는가 | `aria-describedby` · `aria-live` |
| 동영상 자막 · 대본 | 원본 자료 확인 |

실측을 자동화하려면 **axe-core** 같은 도구를 별도로 붙여야 합니다. 위 정적 검사와
성격이 다른 작업입니다.

적용 대상 지침의 최신본과 조항 번호는 **발주처 요구사항으로 확인하세요.**
이 문서는 조문 번호를 담지 않습니다.

---

## 5. 검사 스크립트

`a11y-scan.js` 로 저장해 쓰세요. Node 만 있으면 됩니다.

```javascript
#!/usr/bin/env node
'use strict';
/**
 * 공공기관 사업 — 웹 취약점 · 접근성 정적 검사
 *   node a11y-scan.js [검사할 경로]     (기본: 현재 디렉터리)
 * 의존성 없음. deny 가 하나라도 있으면 종료 코드 1.
 */
const fs = require('fs');
const path = require('path');

const ROOT = path.resolve(process.argv[2] || '.');
const SKIP_DIR = new Set([
  '.git', 'node_modules', 'dist', 'build', 'out', 'coverage',
  'vendor', '.next', '.nuxt', 'target', '.venv', '__pycache__',
]);

const RULES = [
  { id: 'no-dangerous-html', verdict: 'ask', ext: ['.tsx', '.jsx', '.ts', '.js', '.vue'],
    re: /dangerouslySetInnerHTML|\bv-html\b|\.innerHTML\s*=|insertAdjacentHTML\s*\(/i,
    what: '마크업을 문자열로 주입',
    fix: '값이 외부 입력이면 textContent · JSX 보간으로 넣거나 이스케이프하세요. 고정 문자열이면 그대로 두어도 됩니다.' },
  { id: 'no-javascript-url', verdict: 'deny', ext: ['.html', '.jsp', '.jspf', '.tsx', '.jsx', '.vue'],
    re: /(?:href|src|action)\s*=\s*["']?\s*javascript:/i,
    what: 'javascript: URL',
    fix: '이벤트 핸들러를 붙이세요.' },
  { id: 'no-eval', verdict: 'deny', ext: ['.ts', '.tsx', '.js', '.jsx', '.vue'],
    re: /\beval\s*\(|\bnew\s+Function\s*\(|document\.write\s*\(/i,
    what: '문자열을 코드로 실행',
    fix: 'eval · new Function · document.write 를 쓰지 않는 방식으로 바꾸세요.' },
  { id: 'no-target-blank-unsafe', verdict: 'ask', ext: ['.html', '.jsp', '.jspf', '.tsx', '.jsx', '.vue'],
    re: /<a\b(?=[^>]*target\s*=\s*["']?_blank)(?![^>]*\brel\s*=\s*["'][^"']*noopener)[^>]*>/i,
    what: '새 창 링크에 noopener 없음 (탭내빙)',
    fix: 'rel="noopener noreferrer" 를 넣으세요.' },
  { id: 'a11y-img-alt', verdict: 'ask', ext: ['.html', '.jsp', '.jspf', '.tsx', '.jsx', '.vue'],
    re: /<img\b(?![^>]*\balt\s*=)[^>]*>/i,
    what: 'img 대체 텍스트 누락',
    fix: '의미가 있으면 내용을 적고, 장식용이면 alt="" 로 비워 명시하세요.' },
  { id: 'a11y-no-zoom-lock', verdict: 'deny', ext: ['.html', '.jsp', '.jspf'],
    re: /<meta\b[^>]*name\s*=\s*["']viewport["'][^>]*content\s*=\s*["'][^"']*(?:user-scalable\s*=\s*(?:no|0)|maximum-scale\s*=\s*1)/i,
    what: '확대 잠금',
    fix: 'user-scalable=no · maximum-scale=1 을 빼세요.' },
  { id: 'a11y-no-focus-removal', verdict: 'ask', ext: ['.css', '.scss', '.vue', '.tsx', '.jsx'],
    re: /outline\s*:\s*(?:none|0)\b/i,
    what: '초점 표시 제거',
    fix: '대체 초점 스타일(:focus-visible 등)을 함께 넣었는지 확인하세요.' },
  { id: 'a11y-no-positive-tabindex', verdict: 'ask', ext: ['.html', '.jsp', '.jspf', '.tsx', '.jsx', '.vue'],
    re: /tab[Ii]ndex\s*=\s*["']?\{?\s*[1-9]/,
    what: '양수 tabindex',
    fix: '0 또는 -1 만 쓰고, 순서가 필요하면 마크업 순서를 바꾸세요.' },
  { id: 'a11y-no-autoplay', verdict: 'ask', ext: ['.html', '.jsp', '.jspf', '.tsx', '.jsx', '.vue'],
    re: /<(?:video|audio)\b[^>]*\bautoplay\b/i,
    what: '미디어 자동 재생',
    fix: '사용자가 시작하게 하거나 정지 수단을 두세요.' },
  { id: 'a11y-no-blink', verdict: 'deny', ext: ['.html', '.jsp', '.jspf', '.css', '.scss', '.tsx', '.jsx', '.vue'],
    re: /<(?:blink|marquee)\b|text-decoration\s*:\s*blink/i,
    what: '깜빡이는 콘텐츠',
    fix: '제거하세요. 광과민성 발작을 유발할 수 있습니다.' },
  { id: 'a11y-html-lang', verdict: 'ask', ext: ['.html', '.jsp'],
    re: /<html\b(?![^>]*\blang\s*=)[^>]*>/i,
    what: 'html lang 누락',
    fix: 'lang="ko" 를 넣으세요.' },
];

function walk(dir, out = []) {
  let entries;
  try { entries = fs.readdirSync(dir, { withFileTypes: true }); } catch { return out; }
  for (const e of entries) {
    if (SKIP_DIR.has(e.name)) continue;
    const p = path.join(dir, e.name);
    if (e.isDirectory()) walk(p, out);
    else out.push(p);
  }
  return out;
}

const hits = [];
let scanned = 0;

for (const file of walk(ROOT)) {
  const ext = path.extname(file).toLowerCase();
  const rules = RULES.filter((r) => r.ext.includes(ext));
  if (!rules.length) continue;

  let lines;
  try { lines = fs.readFileSync(file, 'utf8').split(/\r?\n/); } catch { continue; }
  scanned++;

  lines.forEach((line, i) => {
    for (const r of rules) {
      const m = line.match(r.re);
      if (m) {
        hits.push({
          rule: r,
          file: path.relative(ROOT, file).split(path.sep).join('/'),
          line: i + 1,
          text: m[0].trim().slice(0, 100),
        });
      }
    }
  });
}

const deny = hits.filter((h) => h.rule.verdict === 'deny');
const ask = hits.filter((h) => h.rule.verdict === 'ask');

console.log(`검사 대상 ${scanned}개 파일 — 즉시 수정 ${deny.length}건 / 확인 필요 ${ask.length}건\n`);

for (const [label, list] of [['즉시 수정 (deny)', deny], ['확인 필요 (ask)', ask]]) {
  if (!list.length) continue;
  console.log(`## ${label}\n`);
  const byRule = new Map();
  for (const h of list) {
    if (!byRule.has(h.rule.id)) byRule.set(h.rule.id, []);
    byRule.get(h.rule.id).push(h);
  }
  for (const [id, group] of byRule) {
    console.log(`### ${id} — ${group[0].rule.what} (${group.length}건)`);
    console.log(`    조치: ${group[0].rule.fix}`);
    for (const h of group.slice(0, 20)) console.log(`    ${h.file}:${h.line}  ${h.text}`);
    if (group.length > 20) console.log(`    … 외 ${group.length - 20}건`);
    console.log('');
  }
}

if (!hits.length) console.log('걸린 항목 없음.');
process.exit(deny.length ? 1 : 0);
```

---

## 6. 보고 형식

검사 결과는 이 형식으로 남기세요.

```markdown
# 웹 취약점 · 접근성 검사 결과 — <프로젝트> (<날짜>)

- 검사 범위: <경로> · <N>개 파일
- 결과: 즉시 수정 <N>건 / 확인 필요 <N>건

## 즉시 수정
| 규칙 | 위치 | 내용 | 조치 |
|---|---|---|---|
| a11y-no-zoom-lock | index.html:2 | 확대 잠금 | user-scalable=no 제거 |

## 확인 필요 — 판단 결과
| 규칙 | 위치 | 판단 | 사유 |
|---|---|---|---|
| a11y-img-alt | main.tsx:14 | 수정함 | 의미 있는 이미지라 alt 추가 |
| a11y-img-alt | main.tsx:31 | 그대로 | 장식용이라 alt="" 로 명시 |
| no-dangerous-html | detail.tsx:52 | 수정함 | 외부 입력이라 textContent 로 변경 |

## 기계로 못 잡는 항목 (4장) 점검
| 항목 | 결과 | 근거 |
|---|---|---|
| 명도 대비 | 통과 | 개발자도구로 전 화면 확인 |
| 키보드 전 기능 | **미검증** | 시간 부족 |
| 스크린리더 낭독 | **미검증** | 보조기기 미확보 |
```

**미검증 항목은 통과로 적지 마세요.** 무엇을 안 봤는지가 남아야 다음에 볼 수 있습니다.
